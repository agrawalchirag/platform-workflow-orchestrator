// Prisma schema — models will be defined as workflow persistence is implemented.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
